<config_plugin>
    <!--
        order of configuration is:
          1.) project name and step name matches
          2.) step name matches and project is *
          3.) project name matches and step name is *
          4.) project name and step name are *
	-->
    
    <config>
        <!-- which projects to use for (can be more then one, otherwise use *) -->
        <project>*</project>
        <step>*</step>
        
        <!-- which vocabulary to use -->
        <vocabulary>Privilegien-Klassen</vocabulary>

        <!-- which field to use for the class name -->
        <class>Klasse</class>

        <!-- which field to search through for the terms -->
        <terms>Schlagworte</terms>
        
        <!-- which metadata field to read to find out the right classes -->
        <metadataTitle>TitleDocMain</metadataTitle>
        
        <!-- which medadata field to create for each found class -->
        <metadataClass>oepmaClass</metadataClass>
        
    </config>

</config_plugin>
